@model IEnumerable<wr_anit_cushman_one.Models.tsg046_tipos_reportes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nb_reporte)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fh_modif)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nb_reporte)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fh_modif)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cd_reporte }) |
            @Html.ActionLink("Details", "Details", new { id=item.cd_reporte }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cd_reporte })
        </td>
    </tr>
}

</table>
