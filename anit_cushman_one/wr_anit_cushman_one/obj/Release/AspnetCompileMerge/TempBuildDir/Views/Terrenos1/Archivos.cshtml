@model IEnumerable<wr_anit_cushman_one.Models.Terreno>

<table class="table">
    <tr>
        
        <th>Nombre Archivo</th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            
            <td>
                @Html.DisplayFor(modelItem => item.tsg045_imagenes_naves.nb_archivo)
            </td>
            <td>
                @Html.ActionLink("Descargar", "EliminarArchivo", "Terrenos1", new { id = "imagen" }) 
                @*@Html.ActionLink("Details", "Details", new { id = item.cd_id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.cd_id })*@
            </td>
        </tr>
    }

</table>